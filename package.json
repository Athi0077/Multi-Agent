{
  "name": "my-agent-2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client/bot",
    "build": "npm run build --prefix client/bot",
    "start": "npm start --prefix server",
    "dev": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client/bot",
    "dev:server": "npm run dev --prefix server"
  }
}
